#include<stdio.h>

void main(){
    char str[30],str1[30];
    gets(str);
    gets(str1);
    int i,len;
    for ( i = 0; str[i] != '\0'; i++)
    {
        /* code */
        len++;
    }
    
    for ( i = 0; str1[1] != '\0'; i++)
    {
        /* code */
        str
    }
    
}